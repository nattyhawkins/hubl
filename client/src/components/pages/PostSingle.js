const PostSingle = () => {
  return (
    <main className='PostSingle'>
      <h1> PostSingle</h1>
      {/* 
      const commentsFromMap = post.comments.map(comment => {
                      const { message, _id } = comment
                      return (
                        <p key={_id}>{message}</p>
                      ) */}
    </main>
  )
}
export default PostSingle